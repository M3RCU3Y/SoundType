<UserControl x:Class="SoundType.App.Controls.VisualKeyboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="KeyboardSurfaceBrush" Color="#10161D"/>
        <SolidColorBrush x:Key="KeyboardKeyBrush" Color="#18232A"/>
        <SolidColorBrush x:Key="KeyboardKeyBorderBrush" Color="#39B77F"/>
        <SolidColorBrush x:Key="KeyboardKeyExcludedBrush" Color="#331B1F"/>
        <SolidColorBrush x:Key="KeyboardKeyExcludedBorderBrush" Color="#EF6464"/>
        <SolidColorBrush x:Key="KeyboardKeyExcludedTextBrush" Color="#FFFFFF"/>
        <Style x:Key="KeyboardKeyButton" TargetType="Button">
            <Setter Property="Height" Value="54"/>
            <Setter Property="MinWidth" Value="33"/>
            <Setter Property="Padding" Value="3,0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F5F7FA"/>
            <Setter Property="Background" Value="{StaticResource KeyboardKeyBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource KeyboardKeyBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.72"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Disabled"
                      CanContentScroll="False">
            <Border Background="{StaticResource KeyboardSurfaceBrush}"
                    BorderBrush="#27323A"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12,16,12,30">
                <Canvas x:Name="KeyboardCanvas"/>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
